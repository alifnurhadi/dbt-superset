name: "BI_superset"
version: "1.0.0"

profile: "BI_superset"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets: # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  BI_superset:
    stg:
      +materialized: view
    int:
      +materialized: view
    mart:
      +materialized: table
